#pragma once

#include <QWidget>

namespace beater {

// Instrument panel (stub for Phase 5)
class InstrumentPanel : public QWidget {
    Q_OBJECT
    
public:
    explicit InstrumentPanel(QWidget* parent = nullptr);
};

} // namespace beater
