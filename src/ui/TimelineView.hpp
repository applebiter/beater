#pragma once

#include <QWidget>

namespace beater {

// Timeline view (stub for Phase 5)
class TimelineView : public QWidget {
    Q_OBJECT
    
public:
    explicit TimelineView(QWidget* parent = nullptr);
};

} // namespace beater
